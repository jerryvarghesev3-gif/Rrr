Output
	•	Firmware version retrieval (hardware/app/boot) successfully integrated from CAN communication layer
	•	Version data mapped and exposed to GUI layer for display
	•	UI fields populated dynamically based on device response
	•	Version information updates automatically after successful device connection
	•	Clear separation maintained between communication logic and UI rendering
	•	Version handling structure designed to support future updates (e.g., post-upgrade refresh)
	•	Consistent and synchronized version display ensured across GUI screens

⸻

Stronger Jira-style Output (recommended)
	•	End-to-end flow established from CAN communication → version retrieval → GUI display
	•	Version management layer implemented to handle multiple firmware components (HW/App/Boot)
	•	Dynamic UI binding enabled for real-time version updates post connection
	•	Scalable version handling mechanism prepared for upgrade validation and comparison use ca
