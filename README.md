✅ Description

Integrate bundle.zip and launcher.exe into the installer package and configure the installer build to include these artifacts correctly.
This ensures that all required components are bundled and available during installation and execution phases.

⸻

✅ Acceptance Criteria
	•	bundle.zip is included in the installer package at the correct location
	•	launcher.exe is added to the installer and accessible post-installation
	•	Installer packaging configuration is updated to include all required artifacts
	•	Packaged installer (.exe) contains both bundle and launcher components
	•	No missing or corrupted files after packaging and extraction
	•	Installer build completes successfully without errors
	•	Artifacts are correctly referenced for use during installation workflow

⸻

✅ Output
	•	Installer package generated with bundle.zip and launcher.exe included
	•	Packaged installer verified for correct file inclusion and structure
	•	Artifacts available for extraction and launcher installation during runtime
	•	Installer build ready for distribution and execution

⸻

🔥 Optional strong line (for review/demo)

“Installer packaging configured to bundle core artifacts ensuring seamless extraction and launcher setup during installation.”
